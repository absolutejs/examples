diff --git a/index.js b/index.js
index d6ed246b18934336c8a74d5ff14653944925e8ab..82a1074549d9eb25fe9df604d7d58f54e492a43e 100644
--- a/index.js
+++ b/index.js
@@ -66,7 +66,11 @@ const isMuslFromChildProcess = () => {
 function requireNative() {
   if (process.env.NAPI_RS_NATIVE_LIBRARY_PATH) {
     try {
-      nativeBinding = require(process.env.NAPI_RS_NATIVE_LIBRARY_PATH);
+      // upstream bug: this branch assigned to outer nativeBinding but never
+      // returned, so requireNative() returned undefined and
+      // `nativeBinding = requireNative()` overwrote what we'd just set.
+      // Returning explicitly makes the env-var override actually work.
+      return require(process.env.NAPI_RS_NATIVE_LIBRARY_PATH);
     } catch (err) {
       loadErrors.push(err)
     }
