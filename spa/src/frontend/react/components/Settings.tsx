export const Settings = () => (
  <div>
    <h2>Settings</h2>
    <p>
      Each sub-route is just a <code>{"<Route>"}</code> declared inside the
      page. <code>react-router</code> handles the dispatch on both server and
      client.
    </p>
  </div>
);
